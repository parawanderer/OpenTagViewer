<?xml version="1.0" encoding="utf-8"?>
<!--
  One device whose passcode could unlock the keychain. Duplicated per recoverable record, the
  same way the sign-in screen builds its SMS buttons.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/icloud_device_button"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:fontFamily="@font/nunito_medium"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="16sp"
        app:icon="@drawable/devices_24px"
        app:iconSize="24dp"
        app:iconTint="?attr/colorOnSecondaryContainer" />

</FrameLayout>
