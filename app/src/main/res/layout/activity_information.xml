<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

<data>
    <variable name="handleClickBack" type="Runnable" />
</data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground"
        tools:context=".InformationActivity">

        <!-- One vertical column rather than a bottom-pinned block overlapping a full-height
             one. The contributor area takes weight, so it uses whatever room is left over and
             scrolls only once its rows no longer fit - which is also why the buttons below it
             can never be pushed off screen as contributors are added. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <include
                android:id="@+id/information_top_toolbar"
                layout="@layout/inline_top_toolbar"
                app:onClickBack="@{handleClickBack}"
                app:pageTitle="@{@string/information}" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:orientation="vertical"
                android:paddingTop="30dp"
                android:textAlignment="center">

                <!--
                    The same mark the sign-in screen opens with, and the same drawable - so it
                    takes the theme's colours here too, including a wallpaper-derived one.

                    Worth the space it occupies: this page had a large empty middle, and the app
                    was named in text three times over without ever being shown. It is also the
                    one screen a person opens on purpose to find out what this thing is.
                -->
                <ImageView
                    android:id="@+id/appLogo"
                    android:layout_width="96dp"
                    android:layout_height="96dp"
                    android:layout_marginBottom="16dp"
                    android:contentDescription="@string/app_name"
                    app:srcCompat="@drawable/opentagviewer_icon_themed" />

                <TextView
                    android:id="@+id/appTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/nunito_bold"
                    android:paddingBottom="20dp"
                    android:text="@string/app_name"
                    android:textAlignment="center"
                    android:textColor="?attr/colorOnSurface"
                    android:textSize="34sp" />

                <TextView
                    android:id="@+id/appVersion"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/nunito_bold"
                    android:text="@string/version_x"
                    android:textAlignment="center"
                    android:textSize="16sp" />

                <TextView
                    android:id="@+id/appAbout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/free_opensource_software_by"
                    android:textAlignment="center"
                    android:textSize="14sp" />

            </LinearLayout>

            <androidx.core.widget.NestedScrollView
                android:id="@+id/contributors_scroll"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:paddingLeft="24dp"
                android:paddingTop="12dp"
                android:paddingRight="24dp"
                android:scrollbars="none">

                <dev.wander.android.opentagviewer.ui.widget.FlowLayout
                    android:id="@+id/contributors_flow"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

            </androidx.core.widget.NestedScrollView>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="40dp">

                <Button
                    android:id="@+id/button3"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:layout_marginBottom="2dp"
                    android:onClick="onClickWiki"
                    android:paddingTop="9dp"
                    android:paddingBottom="9dp"
                    android:text="@string/app_wiki"
                    android:textSize="18sp"
                    app:icon="@drawable/help_center_24px"
                    app:iconSize="22dp" />

                <Button
                    android:id="@+id/button2"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:layout_marginBottom="2dp"
                    android:onClick="onClickGithub"
                    android:paddingTop="9dp"
                    android:paddingBottom="9dp"
                    android:text="@string/github"
                    android:textSize="18sp"
                    app:icon="@drawable/github_24"
                    app:iconSize="22dp" />

            </LinearLayout>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
