<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!--
        For DeviceInfoActivity's "play sound nearby" - direct BLE GATT to an owned accessory,
        bypassing Apple's Find My network. Below API 31 this instead requires the location
        permissions above, which the app already declares. neverForLocation is accurate: the
        scan is only ever matched against a MAC address computed from the accessory's own keys
        (see dev.wander.android.opentagviewer.ble), never used to derive or report a position.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 高德地图SDK所需权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!--
            Maps applications, so "navigate to this tag" can find one.

            Without this the app cannot see any package that handles geo:, and on Android 11
            and up that is not a permission error - resolveActivity simply returns null, as
            though nothing on the phone could open a map. The button then did nothing at all
            and logged, which is indistinguishable from a tag having no location.

            It applied to every device on Android 11 or newer, Google Maps installed or not,
            because package visibility hid the target package too - so naming it explicitly
            could not have worked either. See MapsActivity#onClickNavigateTo.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:name=".OpenAirTagApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenTagViewer"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31"
        >

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
        <service
            android:name=".service.AppNavigationService"
            android:foregroundServiceType="location">
        </service>

        <!-- Reached only when the app cannot name what went wrong - see the class doc. -->
        <activity
            android:name=".ui.error.ErrorReportActivity"
            android:exported="false"
            android:label="@string/error_report_title" />

        <activity
            android:name=".DeviceInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />

        <activity
            android:name=".HistoryViewActivity"
            android:exported="false"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />
        <activity
            android:name=".MyDevicesListActivity"
            android:exported="false"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />
        <activity
            android:name=".FetchFromICloudActivity"
            android:exported="false"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />
        <activity
            android:name=".AppleLoginActivity"
            android:exported="true"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InformationActivity"
            android:exported="false"
            android:label="@string/information"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.OpenTagViewer.GenericGreyishActivity" />
        <!--
            To add your Maps API key to this project:
            1. Open the root project's secrets.properties file
            2. Add this line, where YOUR_API_KEY is your API key:
                MAPS_API_KEY=YOUR_API_KEY
        -->
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- No AMap API key is declared here on purpose. AMap issues keys per developer
             account, bound to a package name and signing fingerprint, and expects the key
             holder to be the app's operator - so users supply their own in Settings and it
             is applied at runtime via MapsInitializer.setApiKey. See the AMap wiki page. -->

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.OpenTagViewer.MapActivity"
            android:screenOrientation="portrait"
            >
        </activity>
    </application>

</manifest>